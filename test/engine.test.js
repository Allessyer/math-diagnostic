import test from'node:test';import assert from'node:assert/strict';import{QUESTIONS,DOMAINS}from'../questions.js';import{coreQuestions,selectFollowups,calculateScores,makeReport,MAX_QUESTIONS}from'../engine.js';const ans=(q,correct)=>({id:q.id,domain:q.domain,difficulty:q.difficulty,selected:correct?q.answer:(q.answer+1)%4,correct,core:q.core});test('valid question bank',()=>{assert.equal(coreQuestions().length,10);for(const q of QUESTIONS){assert.ok(DOMAINS[q.domain]);assert.equal(q.choices.length,4);assert.ok(q.answer>=0&&q.answer<4);assert.ok(q.explanation)}});test('easy and hard adaptation within cap',()=>{const easy=selectFollowups(coreQuestions().map(q=>ans(q,false))),hard=selectFollowups(coreQuestions().map(q=>ans(q,true)));assert.equal(easy.length,5);assert.ok(easy.every(q=>q.id.endsWith('easy')));assert.ok(hard.every(q=>q.id.endsWith('hard')));assert.ok(coreQuestions().length+easy.length<=MAX_QUESTIONS)});test('weighted scores',()=>{const qs=QUESTIONS.filter(q=>q.domain==='arithmetic').slice(0,2),s=calculateScores([ans(qs[0],true),ans(qs[1],false)]);assert.equal(s.arithmetic.score,50);assert.equal(s.algebra.score,null)});test('privacy-safe report',()=>{const r=makeReport([ans(coreQuestions()[0],true)],'2026-01-01');assert.equal(r.totalQuestions,1);assert.deepEqual(Object.keys(r.answers[0]),['id','domain','difficulty','selected','correct'])});
